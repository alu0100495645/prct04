#!/usr/bin/python
#!encoding: UTF-8

a = float(raw_input('Valor de a: '))
b = float(raw_input('Valor de b: '))
a * x + b = 0
print 'Solucion: ', x

#No funciona, me sale lo siguiente al no asignar un operador :SyntaxError: can't assign to operator#
